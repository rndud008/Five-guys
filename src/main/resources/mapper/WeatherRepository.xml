<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lec.spring.repository.WeatherRepository">
    <insert id="insertWeather" parameterType="com.lec.spring.domain.WeatherDTO">
        INSERT INTO short_weather (category, fcstDate, fcstTime, fcstValue, url)
        VALUES (#{category}, #{fcstDate}, #{fcstTime}, #{fcstValue}, #{url})
    </insert>
</mapper>
