<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lec.spring.repository.WeatherRepository">

    <insert id="insertWeather" parameterType="com.lec.spring.domain.WeatherDTO" flushCache="true"
            useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        INSERT INTO short_weather (areacode, last_call_api_id, category, fcstDate, fcstTime, fcstValue)
        VALUES (#{areacode.areacode}, #{lastCallApiData.id} ,#{category}, #{fcstDate}, #{fcstTime}, #{fcstValue})
    </insert>
</mapper>
