<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lec.spring.repository.TravelClassDetailRepository">
    <insert id="insertTravelClassDetail">
        INSERT INTO travel_class_detail(id, travel_type_id, name, code, decode)
        VALUES (#{id}, #{travel_type_id}, #{name}, #{code}, #{decode})
    </insert>

</mapper>